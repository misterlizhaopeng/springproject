代理模式：分为静态代理和动态代理
代理模式的概念：
    具体的执行者被委托一个执行者A执行，这个执行者A（或者说只是发命令不干实事的角色）就是一个代理对象；

_static:静态代理的测试代码
    缺点：在编译阶段，一个代理对象只能代理一个功能类


_dynamic:动态代理的测试代码
    动态代理又分为 jdk动态代理和cglib动态代理，jdk动态代理必须要求被代理的对象实现一个接口，然而cglib则不需要；
    优点：避免了静态代理一个代理对象只能代理一个功能类的弊端，其发代理功能是发挥在运行阶段；
   jdk动态代理的实现步骤：
        1.bind 把真是对象和代理对象进行绑定；
        2.invoke；调用真是对象的具体动作，都会执行invoke方法，实现具体的代理控制逻辑